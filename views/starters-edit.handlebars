<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <form method="POST" action="/starters/{{starter.slug}}?_method=PUT">
        <fieldset>
    <h1>Edit</h1>
    <legend>Please, start again, from the beginning...</legend>

    <h2>{{starter.title}}</h2>

    <h3>{{starter.content}}</h3>

    <label for="starter-content"></label>
    <input type="text" name="content" class="form-control" id="starter-content" placeholder="{{starter.content}}">

  </fieldset>

        <div class="finished_button">
      <div class="form-group">
        <button class="btn btn-primary" type="submit">Save changes!</button>
      </div>
    </form>
      </div>



    <div class='edit'>
         <span style="float:left">
         <p><form method="" action="/starters/{{starter.slug}}">
             <button class="btn btn-primary" type="submit">Back</button>
         </form></p></span>
     <span style="float:right">
     <p><form method="POST" action="/starters/{{starter.slug}}?_method=DELETE">
         <button class="btn btn-primary" type="submit">Delete ENTIRE Story Thread</button>
     </form></p></span>
   </div>


        <div class='edit'>

         <span style="float:center">
             <h3>Are you finished writing the story?</h3>
             <h3>Once you mark the story as finished it cannot be added to by you or your friends and will be moved to the finished section.</h3>
        <div class="finished_button">
         <form method="POST" action="/starters/{{starter.slug}}/finished?_method=PUT">
             <button class="btn btn-primary" type="submit">Finished</button>
         </form>
        </div>
        </span>

        </div>

    </div>
</div>
