<div class="row">
<div class="col-xs-12 comment-indent">
  <!-- <div class="col-sm-6 col-sm-offset-3"> -->
    <h1>{{starter.title}}</h1>
    <a href="{{starter._id}}/edit" class="lead"><p>{{starter.content}}</p></a>
  </div>
  <div class="col-xs-12 comment-indent">
     <p class="text-right">{{starter.author.username}}</p>
  </div>


  {{#each starter.threads}}
    {{> thread thread=this starterId=../starter._id}}
  {{/each}}

<!-- <a href="user/{{user._id}}" class="lead">{{starter.author.username}}</a> -->


  <form action="/starters/{{starter._id}}/threads" method="post">
  <textarea class='form-control' name="content" placeholder="One more thread..."></textarea>
  <div class="text-right">
    <button type="submit" class="btn btn-primary">Add to the tale!</button>
  </div>
</form>

</div>
